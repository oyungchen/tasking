# LAN Peer Discovery & Task Assignment — Design Spec

**Date:** 2026-05-19
**Status:** Approved

## Context

The task management app is currently single-user. Multiple people on the same LAN running their own instances have no way to know about each other or collaborate. The goal is to enable LAN peer discovery so users can see who else is online and assign tasks to each other, with status tracking.

## Overview

```
┌─────────────────┐     mDNS register/browse     ┌─────────────────┐
│  Instance A      │◄──────────────────────────► │  Instance B      │
│  (Alice)         │                              │  (Bob)           │
│                  │     POST /peer/assign ──────►│                  │
│  tasks.json      │◄──── POST /peer/status-update│  tasks.json      │
│                  │                              │                  │
│  assigned_to=Bob │                              │  assigned_by=Alice│
└─────────────────┘                              └─────────────────┘
```

Two components:
1. **Discovery** — mDNS (Bonjour/Zeroconf) so instances find each other automatically
2. **Direct HTTP** — peer-to-peer task assignment and status sync, no central server

## Modules

### 1. Instance Identity (`tasks/.instance.json`)

```json
{
  "instance_id": "uuid4-generated-on-first-run",
  "display_name": "Alice"
}
```

- `instance_id`: generated once via `uuid.uuid4()`, persisted across restarts
- `display_name`: defaults to `socket.gethostname()`, editable via new settings UI (a small inline edit next to the peers list)

New file: `identity.py` — `get_or_create_identity()`, `set_display_name(name)`

### 2. mDNS Service Discovery (`peer.py`)

**Library:** `zeroconf` (PyPI: `python-zeroconf>=0.130`)

Add to `requirements.txt`.

**Service type:** `_taskdone._http._tcp.local.`

On startup:
1. **Register** own service on the LAN: port=Flask port, TXT records: `instance_id`, `display_name`
2. **Browse** for other `_taskdone._http._tcp.local.` services
3. On service added/removed → update in-memory peer list
4. On service updated (display_name changed) → update in-memory peer list

**`peer.py`** exposes:
- `start_discovery(port)` — starts the Zeroconf ServiceBrowser + registers own service
- `stop_discovery()` — cleanup
- `get_peers()` → `[{"instance_id", "display_name", "host", "port", "is_self"}]`

Peers are stored in memory only (no persistence needed — discovery is real-time).

### 3. Peer HTTP API (`server.py` new routes)

Three new endpoints:

#### `POST /peer/assign`
```json
// Request
{
  "from_instance": "uuid-abc",
  "from_display": "Alice",
  "task": { ... full task object ... }
}
// Response: 201 { "task_id": "...", "ok": true }
```
Creates the task locally on the receiver with `assigned_by` set.

#### `POST /peer/status-update`
```json
// Request
{
  "task_id": "20260519...",
  "status": "processing",
  "from_instance": "uuid-xyz"
}
// Response: 200 { "ok": true }
```
Updates the local copy of an assigned-out task's status. Only accepted from the instance the task was assigned to.

#### `GET /peer/info`
```json
// Response: 200
{
  "instance_id": "uuid-xyz",
  "display_name": "Bob",
  "peer_api_version": 1
}
```
Health/identity check before assigning.

### 4. Data Model Changes (`models.py`)

Task gains two optional fields:

```python
assigned_by: Optional[dict] = None   # {"instance_id": "...", "display_name": "..."}
assigned_to: Optional[dict] = None   # {"instance_id": "...", "display_name": "..."}
```

These are persisted as part of the task dict in `tasks.json`.

**Semantics:**
| Scenario | `assigned_by` | `assigned_to` | Visual |
|---|---|---|---|
| Own task | None | None | Normal |
| Received from Alice | `{id, "Alice"}` | None | Highlighted border + "From Alice" badge |
| Sent to Bob | None | `{id, "Bob"}` | Muted + "Assigned to Bob" badge |

**Status sync for assigned tasks:**
When a task with `assigned_by` changes status → POST `/peer/status-update` to the source instance. The source instance finds the matching task by task ID (the ID is generated by the assigner and preserved on the receiver) and updates its local `status` field.

Task ID generation: the assigner generates the ID (using its own `%Y%m%d%H%M%S`+random scheme), sends it in the task payload. The receiver uses that exact ID — this ensures both sides reference the same task.

### 5. Frontend Changes (`web/`)

#### 5a. Peers Panel

A thin bar or dropdown below the header showing:
```
Online:  Alice (you)  |  Bob  |  Charlie (2)
```

Implement as a new `<div id="peers-bar">` in `index.html`, styled inline with the header. Clicking a peer name selects them as the assignment target.

Polling: `GET /api/peers` returns the current peer list (forwarded from `peer.py`). Frontend polls every 5 seconds.

#### 5b. Assign UI

In the task detail modal, add an "Assign to..." button (only visible when peers are online and task is not already assigned). Clicking it shows a small dropdown of online peers. Confirming sends the assignment:

```
POST /api/tasks/<id>/assign  { "to_instance": "uuid-xyz" }
```

This endpoint (on the assigner's server):
1. Looks up the peer's host:port from the in-memory peer list
2. HTTP POSTs to `http://<peer_host>:<peer_port>/peer/assign` with the task data
3. On success, updates the local task: sets `assigned_to`, removes it from main grid view (or shows grayed out)

#### 5c. Task Card & Detail Changes

- Cards with `assigned_by`: light left border accent color + small "From Alice" label
- Cards with `assigned_to`: muted opacity + "→ Bob" label
- Detail modal: shows assignment info with current status for outgoing assignments

#### 5d. Name Editing

A small edit icon next to "You" in the peers bar that opens an inline input to change `display_name`. Saves via `POST /api/identity/name` → updates mDNS TXT record so peers see the new name.

### 6. New API Endpoints (own server)

| Method | Path | Purpose |
|---|---|---|
| GET | `/api/peers` | Returns current online peer list |
| POST | `/api/tasks/<id>/assign` | Assign a task to a peer |
| POST | `/api/identity/name` | Set display name |
| GET | `/api/identity` | Get current identity info |

## Files Changed

| File | Change |
|---|---|
| `identity.py` | **New** — instance identity management |
| `peer.py` | **New** — mDNS discovery + peer registry |
| `models.py` | Add `assigned_by`, `assigned_to` fields to Task |
| `storage.py` | No changes (dict-based fields auto-persist) |
| `server.py` | New routes: `/api/peers`, `/api/tasks/<id>/assign`, `/api/identity`, `/peer/*` |
| `requirements.txt` | Add `zeroconf>=0.130` and `requests` (stdlib `urllib` also fine) |
| `web/index.html` | Add peers bar, assign dropdown in detail modal |
| `web/app.js` | Peers polling, assign flow, task card annotation rendering |
| `web/styles.css` | Peers bar styles, assignment badges, muted card styles |

## Verification

1. Start two instances on different ports: `PORT=8080 python server.py` and `PORT=8081 python server.py` (different `tasks/` dirs or same dir — ideally test with separate data)
2. Confirm both appear in each other's `/api/peers`
3. Assign a task from A to B — verify it appears in B's grid with "From Alice" label
4. On B, move the task to processing → check A sees status update
5. On B, move the task to done → check A sees completion
6. Stop one instance — verify it disappears from peer list within ~10 seconds
7. Change display name on A — verify B sees the new name